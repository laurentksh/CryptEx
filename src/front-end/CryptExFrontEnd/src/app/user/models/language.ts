export interface Language {
    /**
     * Language identifier (always lowered, e.g: en-us)
     */
    Id: string;

    /**
     * The language's name, written in it's own language (e.g: Français, Deutsch)
     */
    DisplayText: string;
}
